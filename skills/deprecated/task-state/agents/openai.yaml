interface:
  display_name: "Deprecated: Task State"
  short_description: "Archived; use the configured tracker instead"
  default_prompt: "This skill is deprecated. Use the configured external tracker or $local-work-tracker for new work."
policy:
  allow_implicit_invocation: false
