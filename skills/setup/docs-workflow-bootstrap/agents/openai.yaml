interface:
  display_name: "Docs Workflow Bootstrap"
  short_description: "Scaffold optional Feature-centered engineering docs"
  default_prompt: "Use $docs-workflow-bootstrap to scaffold only the Feature-centered docs convention I explicitly approve."
policy:
  allow_implicit_invocation: false
