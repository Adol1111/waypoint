interface:
  display_name: "Waypoint Workflow"
  short_description: "Recommend one next skill without invoking it"
  default_prompt: "Use $waypoint-workflow to inspect this repository and recommend exactly one next skill without doing its work."
policy:
  allow_implicit_invocation: false
