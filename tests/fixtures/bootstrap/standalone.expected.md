# Expected behavior

- It proposes standalone Features under `docs/work/features/<feature>/`.
- It does not create or require a Milestone.
- Feature IDs remain globally unique and child Tasks stay inside their Feature.
- It keeps `requirements.md`, the active Feature view, and `completed.md` available without inventing placeholder work.
- It asks the user to confirm the exact scaffold before creating it.
