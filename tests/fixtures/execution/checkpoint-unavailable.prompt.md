# Repository evidence

The supplied task is implemented and verified, but its changes overlap unrelated
user edits that cannot be separated safely. No commit has been created. The
agent is about to move to another task.

# Request

Use `task-execution-simple` to finish the current task safely.
