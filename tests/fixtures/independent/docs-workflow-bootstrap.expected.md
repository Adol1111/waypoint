# Expected contract

- The skill must not require a docs layout or another skill.
- It confirms and creates only the accepted context and decision locations.
- It does not create `docs/tasks/`, placeholder ADRs, a lifecycle, labels, or branch rules.
- It explains that every standalone skill still works outside the convention.
