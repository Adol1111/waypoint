# Prompt

This repository has no shared engineering documentation. No `docs/` directory exists.

The team explicitly wants lightweight places for terminology and occasional ADRs, but not local task tracking.

Invoke `docs-workflow-bootstrap` directly.
