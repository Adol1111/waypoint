# Expected contract

- The skill must not require a docs layout or another skill.
- It updates a suitable existing glossary if discovered; otherwise it proposes a small local Markdown glossary.
- It records the durable semantic distinction and excludes temporary implementation discussion.
- It does not create an ADR unless all ADR thresholds are independently satisfied.
