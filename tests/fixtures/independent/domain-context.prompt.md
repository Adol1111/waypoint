# Prompt

This small Go repository has only `README.md` and source files. No `docs/` directory exists.

The terms “lease” and “reservation” are used interchangeably, but a lease expires while a reservation does not. Preserve that distinction for future contributors.

Invoke `domain-context` directly.
