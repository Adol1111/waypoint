# Prompt

The repository keeps task notes in `work/rotate-keys.md`. No `docs/` directory exists.

Plan a dual-read credential migration across the API and worker, including rollback.

Invoke `implementation-plan` directly.
