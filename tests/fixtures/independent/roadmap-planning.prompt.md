# Prompt

This repository tracks direction in `ROADMAP.md`. No `docs/` directory exists.

Turn “operators can rotate API credentials without downtime” into delivery slices.

Invoke `roadmap-planning` directly.
