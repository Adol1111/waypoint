# Prompt

The repository has `CHANGE.md` with a complete requested behavior and tests beside the code. No `docs/` directory exists.

Implement that scoped change directly.

Invoke `task-execution-simple` directly.
