# Prompt

The repository keeps feature notes in `work/export-audit.md`. No `docs/` directory exists. No issue tracker is configured.

Split the agreed export-audit feature into independent tasks that two different coding agents can implement safely.

Invoke `task-planning` directly. Do not implement anything.
