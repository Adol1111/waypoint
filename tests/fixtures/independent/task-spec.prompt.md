# Prompt

The repository has `issues/42.md` but no specification convention. No `docs/` directory exists.

Specify the requested retry-budget change so another engineer can review and implement it without this conversation.

Invoke `task-spec` directly.
