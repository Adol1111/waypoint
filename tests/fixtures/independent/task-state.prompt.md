# Prompt

The only task artifact is `TODO.md`. No `docs/` directory exists.

The credential rotation task is blocked because the staging KMS grant is missing. Record recoverable state.

Invoke `task-state` directly.
