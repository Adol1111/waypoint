# Prompt

The repository tracks the task in `issues/57.md`. Its behavioral specification requires safe idempotent payment retries, but architecture, persistence, concurrency handling, and verification seams are unresolved. No `docs/` directory exists.

Design the review-critical technical approach without writing implementation code or a file-by-file recipe.

Invoke `technical-design` directly.
