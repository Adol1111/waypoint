# Repository evidence

A new Milestone is being planned. Backlog contains three items:

- Token rotation now matches the new Milestone outcome and its reconsideration
  signal is satisfied.
- Retry telemetry was already delivered by a completed task with durable
  verification.
- Regional failover still has the same dependency and reconsideration signal as
  when it was deferred.

The first two items each have a source entry in an earlier Milestone's
Discovered work.

# Request

Use `milestone-workflow` to retrieve relevant backlog work without turning the
backlog into a review log.
