# Repository evidence

Credential Rotation is active. During one task, verification reveals that retry metrics cannot distinguish budget exhaustion. Nobody has decided whether this is required now or later.

# Request

Use `milestone-workflow` to preserve the finding.
