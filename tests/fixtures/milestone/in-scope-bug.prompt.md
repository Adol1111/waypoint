# Repository evidence

The active task requires token refresh failure to preserve the previous usable
token. During implementation, its focused test reveals that the code clears the
token before persistence succeeds.

# Request

Use `milestone-workflow` to decide where this finding belongs.
