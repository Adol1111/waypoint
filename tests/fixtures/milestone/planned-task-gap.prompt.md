# Repository evidence

During an unrelated current task, verification reveals that retry metrics cannot
distinguish budget exhaustion. A later task named "Improve retry metrics" already
exists in the same active Milestone, but its acceptance mentions only counting
retry attempts.

# Request

Use `milestone-workflow` to place the finding without losing it.
