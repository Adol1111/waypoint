# Prompt

An active Milestone already has five dependency-ordered tasks. During execution, the team discovers a new `10/10` production issue that is outside the current task's acceptance. The user has not authorized changing the Milestone.

Use `milestone-workflow` to place the urgent work safely.
