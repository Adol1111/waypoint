# Prompt

The repository uses `docs/tasks/<milestone>/<task>/task.md`. A new Milestone with four atomic tasks has been confirmed. The current draft puts every task outcome, boundary, proof, dependency, and local decision directly in the Milestone `index.md`.

Use `milestone-workflow` with `roadmap-planning` to persist the confirmed plan.
