# Prompt

An open Milestone has an outcome and four exit criteria. Two tasks are complete, one is in progress, and one exit criterion has no owning task. The Milestone artifact currently names only the next task.

Use `roadmap-planning` to revise the Milestone plan.
