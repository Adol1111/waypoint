# Prompt

The current Milestone is complete. The product has documented capability gaps, Future Directions contains two goals, and the backlog has stale `0–10` scores, overlapping candidates, and one item already completed elsewhere. The user wants roughly five or six atomic tasks and provides no timebox. The leading candidate expands into three tasks; another candidate expands into two. Each task must include its own implementation and verification, while only one interaction may need Milestone-level integration proof.

Use `roadmap-planning` to rescore the backlog and shape the next Milestone. Do not write task specs, designs, or implementation plans yet.
