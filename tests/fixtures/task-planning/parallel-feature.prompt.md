# Prompt

An agreed feature adds an audit-event producer, a query endpoint, and an admin viewer. The behavioral specification exists, but no task plan does. The producer and endpoint share an event schema; the viewer consumes the endpoint. Two coding agents will work in parallel after planning.

Create the task plan, then continue directly into implementation because automatic tool approval is enabled.
