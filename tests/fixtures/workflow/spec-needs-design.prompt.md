# Repository evidence

- `engineering/changes/payment-retry/spec.md` completely defines observable retry behavior and acceptance.
- The repository has no settled choice for persistence ownership, concurrent duplicate requests, the internal state model, or the verification seam.
- No migration or rollout order is yet being planned.

# Request

What should I use next before implementation?
